---
description: Build the Android APK Preview using EAS Local Build
---

1. Navigate to the project directory
// turbo
2. Run the EAS build command locally
```bash
npx eas-cli build -p android --profile preview --local
```
